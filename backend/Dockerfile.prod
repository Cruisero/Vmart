# 生产环境 Dockerfile
FROM node:20-alpine

# 安装 Prisma 所需的 OpenSSL 和备份所需的 mysql-client
RUN apk add --no-cache openssl mysql-client

WORKDIR /app

# 复制 package 文件
COPY package*.json ./
COPY prisma ./prisma/

# 只安装生产依赖 + Prisma (需要 dev 依赖)
RUN npm ci

# 生成 Prisma 客户端
RUN npx prisma generate

# 复制源代码
COPY . .

# 创建上传目录
RUN mkdir -p /app/uploads/products/original /app/uploads/products/large /app/uploads/products/medium

# 暴露后端端口
EXPOSE 8080

# 启动生产服务器
CMD ["node", "server.js"]
